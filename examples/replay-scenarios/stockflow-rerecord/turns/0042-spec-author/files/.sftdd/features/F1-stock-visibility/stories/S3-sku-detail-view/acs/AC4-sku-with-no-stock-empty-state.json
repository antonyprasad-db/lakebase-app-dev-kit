{
  "id": "AC4-sku-with-no-stock-empty-state",
  "given": "a SKU identifier that has no stock records at any location (the only observable ground for absence, since stock_records is the sole data store and no separate SKU registry exists to distinguish a known-but-empty SKU from an unknown one)",
  "when": "the warehouse team member requests that SKU's detail view",
  "then": "an explicit \"No stock for this SKU\" state is shown instead of a blank page or a null error",
  "status": "draft",
  "layer": "E2E",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC1-AC3 concern a SKU that has stock rows and its optional detail; AC4 concerns the disjoint case of zero matching stock rows, asserting an explicit empty state grounded in observable data (no rows) rather than a blank page or crash. It replaces the earlier contradictory known-vs-unknown (200-vs-404) split, which is not implementable without a SKU registry table that the architecture does not declare. This empty state can be missing while populated SKUs render correctly, so it fails independently."
  }
}
