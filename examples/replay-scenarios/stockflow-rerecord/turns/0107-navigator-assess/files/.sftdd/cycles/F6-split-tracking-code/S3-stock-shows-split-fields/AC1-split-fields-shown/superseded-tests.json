{
  "tests": [
    "tests/step_defs/test_S1_file_stock.py",
    "tests/step_defs/test_S1_split_columns_migration.py",
    "tests/step_defs/test_S3_sku_detail_view.py",
    "tests/test_S1_fitness.py",
    "tests/test_S1_migration.py",
    "tests/test_S1_split_fitness.py",
    "tests/test_S2_reversible_down_migration.py",
    "tests/test_S3_stock_shows_split_fields_fitness.py"
  ],
  "reason": "AC1 retires inventory_code from the API surface (boundary models now expose batch_number + serial_number only). The migration that dropped inventory_code also makes it unavailable in the downgrade path, so all tests that assert inventory_code EXISTS as a column or in a response, seed rows via INSERT INTO ... inventory_code, or test reversibility properties of the now-obsolete combined column are superseded. Latest AC wins (software-design-principles hard rule 8)."
}
