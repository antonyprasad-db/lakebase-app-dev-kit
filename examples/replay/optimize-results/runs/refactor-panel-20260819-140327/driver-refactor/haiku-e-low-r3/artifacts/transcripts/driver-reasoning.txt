Refactor complete. Changes made:

1. **client/src/pages/HomePage.tsx** – Applied design-vocabulary CSS classes:
   - Added `className="table"` to `<table>` (design-token styles for header, border-collapse)
   - Added `className="empty-state"` to empty-state `<p>` (centered, padded, on-brand treatment)
   - Added `className="table__num"` alongside existing `className="stock-table__quantity"` on quantity cells (text-align:right, tabular-nums, monospace)

2. **app/repositories/__init__.py** – Fixed backend import: exported `stock` submodule so service layer can access repository functions.

All tests pass: client (19), backend S2 (2), architecture fitness (4).