{
  "id": "AC4-split-columns-removed",
  "given": "a stock record on the split schema after the down path has recombined the code",
  "when": "the down path completes",
  "then": "the batch_number and serial_number columns are no longer present on the stock record, restoring the pre-split schema shape",
  "status": "draft",
  "layer": "Infra",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "prior ACs assert the combined column and its value are restored; this asserts the distinct outcome that the split columns are dropped, which can be failed independently by reconstructing inventory_code while leaving batch_number and serial_number behind."
  },
  "architectural_notes": "Infra: the final DDL step of the Alembic downgrade() on stock_records, owned by the migration revision. Ordering is the architectural guarantee (PI3-rows-preserved, additive-then-drop symmetry with S1): the backfill of inventory_code (AC1/AC2/AC3) MUST complete before batch_number and serial_number are dropped, so no in-flight data is lost when the columns are removed. Restores the exact pre-split schema shape. No boundary/service involvement. Verified by a real-branch test asserting both split columns are absent after downgrade while inventory_code remains populated (R4)."
}
