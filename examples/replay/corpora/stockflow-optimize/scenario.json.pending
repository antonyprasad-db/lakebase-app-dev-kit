{
  "name": "stockflow-optimize",
  "description": "Wall-clock optimization re-record of the StockFlow inventory app (design + build lanes live, incl the DBA design-lane role and the UX-track client). One sprint ships F1 record-stock visibility. Captured via the per-handoff optimization harness (champion walk): at each role handoff, candidate variants (model / effort / session warmth / loop granularity, plus prompt / reference / inject-vs-scan / tool-scope content variants) are tried from an identical pre-turn state, the fastest gate-passing candidate wins, and only the winner survives in this corpus. Every attempt is preserved under experiments/; the surviving turns/ + recorded-artifacts/ + recorded-build/ are a clean, replayable, internally-consistent corpus. Goal: same-quality artifacts with less wall-clock.",
  "tiers": 2,
  "uiTrack": true,
  "language": "python",
  "runner": "self-hosted",
  "features": [
    { "id": "F1-stock-visibility", "buildReplay": true, "summary": "additive: record-stock form + stock-by-location home table + sku detail view" }
  ]
}
