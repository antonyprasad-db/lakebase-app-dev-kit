# Test list: F6-split-tracking-code
Ordered for: design-momentum

- [x] T1: after the add-and-backfill migration runs on a seeded stock row, that row exposes batch_number and serial_number as distinct, separately addressable columns  (AC1-columns-exist)
- [x] T2: after the add-and-backfill migration, location and inventory_code remain present and unchanged on a seeded stock row  (AC1-columns-exist)
- [x] T3: after a single-step downgrade followed by a single-step upgrade on the real branch database, the stock table has a batch_number column and a serial_number column  (AC1-columns-exist)
- [x] T4: the API boundary module (app/routes/) imports only from the service layer and contains no SQLAlchemy session or ORM imports  (AC1-columns-exist)
- [x] T5: the service module (app/services/) contains no SQLAlchemy session or ORM model imports; only the repository layer imports from the ORM  (AC1-columns-exist)
- [x] T6: DATABASE_URL is read from the environment variable and is not hard-coded in any application module  (AC1-columns-exist)
- [x] T7: after the migration, a stock row whose inventory_code was a hyphen-delimited location-batch-serial code has batch_number set to the second hyphen-delimited segment  (AC2-backfill-parses-conforming-code)
- [x] T8: after the migration, a stock row whose inventory_code was a hyphen-delimited location-batch-serial code has serial_number set to the third hyphen-delimited segment  (AC2-backfill-parses-conforming-code)
- [x] T9: after the migration, a stock row whose inventory_code has fewer than three hyphen-delimited segments has batch_number left NULL  (AC3-nonconforming-code-leaves-nulls)
- [x] T10: after the migration, a stock row whose inventory_code has fewer than three hyphen-delimited segments has serial_number left NULL  (AC3-nonconforming-code-leaves-nulls)
- [x] T11: after the migration, a nonconforming stock row is still present in the table with its sku unchanged  (AC3-nonconforming-code-leaves-nulls)
- [x] T12: inserting a stock row with batch_number=NULL and serial_number=NULL against the real branch database succeeds without a constraint violation  (AC3-nonconforming-code-leaves-nulls)
- [x] T13: inserting a stock row with sku=NULL against the real branch database raises a NOT NULL constraint violation  (AC3-nonconforming-code-leaves-nulls)
- [x] T14: after the migration, the stock row count scoped to the test's own seeded rows is identical before and after the upgrade  (AC4-every-row-survives)
- [x] T15: after the migration, each seeded stock row retains its original sku value unchanged  (AC4-every-row-survives)
- [x] T16: after the migration, each seeded stock row retains its original location value unchanged  (AC4-every-row-survives)
- [x] T17: after the migration, each seeded stock row retains its original quantity value unchanged  (AC4-every-row-survives)
- [x] T18: inserting two stock rows with the same (sku, location) pair against the real branch database raises a UniqueViolation error  (AC4-every-row-survives)
- [x] T19: the migration upgrade (add batch_number and serial_number) and its backfill UPDATE execute inside a single transaction; injecting a failure mid-backfill leaves neither column nor any partial backfill on the real branch database  (AC4-every-row-survives)
- [x] T20: the count of test-owned stock rows on the real branch database is identical before and after the upgrade migration  (AC4-every-row-survives)
- [x] T21: for every stock row seeded before the upgrade migration, the sku, location, and quantity values are unchanged after the upgrade runs against the real branch database  (AC4-every-row-survives)
- [x] T22: when the integrity probe is run after backfill on a mixed table of conforming and nonconforming codes, it returns the exact count of rows whose batch_number and serial_number are both NULL  (AC5-integrity-probe-reports-nonconforming-count)
- [x] T23: after the S2 drop migration runs against a real branch DB seeded with a uuid-suffixed stock row, the inventory_code column is absent from the stock table schema  (AC1-column-dropped)
- [x] T24: after the S2 drop migration runs, the batch_number column is still present on the stock table  (AC1-column-dropped)
- [x] T25: after the S2 drop migration runs, the serial_number column is still present on the stock table  (AC1-column-dropped)
- [x] T26: after the S2 drop migration runs, the location column is still present on the stock table  (AC1-column-dropped)
- [x] T27: after the S2 drop migration runs against a branch DB seeded with rows using uuid-suffixed location keys, the delta row count for the test's own seed rows is zero  (AC2-every-row-survives)
- [x] T28: after the S2 drop migration runs, a seeded stock row retains its original batch_number value  (AC2-every-row-survives)
- [x] T29: after the S2 drop migration runs, a seeded stock row retains its original serial_number value  (AC2-every-row-survives)
- [ ] T30: after the S2 drop migration runs, a seeded stock row retains its original location value  (AC2-every-row-survives)
- [ ] T31: after the S2 down-migration is applied against a branch DB in post-drop state, inventory_code column is present in the stock table schema  (AC3-down-migration-reconstructs-code)
- [ ] T32: after the S2 down-migration is applied, inventory_code for a seeded row equals the concatenation of location, batch_number, and serial_number as the combined-code formula defines  (AC3-down-migration-reconstructs-code)
- [ ] T33: after a single-step S2 downgrade followed by a single-step S2 upgrade on the real branch database, inventory_code column is absent from the stock table schema (the forward schema is faithfully recreated)  (AC3-down-migration-reconstructs-code)
- [ ] T34: given a stock record with batch_number and serial_number populated, when the detail API endpoint is called for that record, then the JSON response contains batch_number as a distinct top-level field with its correct value  (AC1-detail-view-shows-batch-and-serial)
- [ ] T35: given a stock record with batch_number and serial_number populated, when the detail API endpoint is called for that record, then the JSON response contains serial_number as a distinct top-level field with its correct value  (AC1-detail-view-shows-batch-and-serial)
- [ ] T36: the stock detail API boundary returns batch_number and serial_number as distinct fields in a JSON response body with no server-rendered HTML (NFR-F6-spa-json-boundary)  (AC1-detail-view-shows-batch-and-serial)
- [ ] T37: navigating to /stock/:id in <App> renders the detail view with batch number in a labeled field carrying the card design-guide class  (AC1-detail-view-shows-batch-and-serial)
- [ ] T38: navigating to /stock/:id in <App> renders the detail view with serial number in a labeled field carrying the card design-guide class  (AC1-detail-view-shows-batch-and-serial)
- [ ] T39: batch number and serial number appear as two distinct separately labeled fields (not merged into one element) in the detail view at /stock/:id  (AC1-detail-view-shows-batch-and-serial)
- [ ] T40: given stock records exist, when the stock list API endpoint is called, then each record in the JSON response contains batch_number as a distinct field  (AC2-list-view-shows-batch-and-serial)
- [ ] T41: given stock records exist, when the stock list API endpoint is called, then each record in the JSON response contains serial_number as a distinct field  (AC2-list-view-shows-batch-and-serial)
- [ ] T42: navigating to /stock in <App> renders the list view with each stock record showing a batch number field carrying the stock-table design-guide class  (AC2-list-view-shows-batch-and-serial)
- [ ] T43: navigating to /stock in <App> renders the list view with each stock record showing a serial number field carrying the stock-table design-guide class  (AC2-list-view-shows-batch-and-serial)
- [ ] T44: batch number and serial number appear as separate fields per row in the list view at /stock  (AC2-list-view-shows-batch-and-serial)
- [ ] T45: given a stock record whose batch_number is NULL, when the detail API endpoint is called, then batch_number is present in the JSON response as null rather than absent or causing an error  (AC3-empty-batch-serial-renders-blank-state)
- [ ] T46: given a stock record whose serial_number is NULL, when the detail API endpoint is called, then serial_number is present in the JSON response as null rather than absent or causing an error  (AC3-empty-batch-serial-renders-blank-state)
- [ ] T47: given a stock record whose batch_number and serial_number are NULL, when the stock list API endpoint is called, then batch_number and serial_number appear as null in the JSON response and the remainder of the record fields are still present  (AC3-empty-batch-serial-renders-blank-state)
- [ ] T48: the stock detail API returns batch_number as JSON null (not omitted, not an error) when the column value is NULL on the real branch database  (AC3-empty-batch-serial-renders-blank-state)
- [ ] T49: when a stock record has NULL batch_number, the detail view at /stock/:id renders an element with class empty-state instead of an error or broken layout  (AC3-empty-batch-serial-renders-blank-state)
- [ ] T50: when a stock record has NULL serial_number, the detail view at /stock/:id renders an element with class empty-state instead of an error or broken layout  (AC3-empty-batch-serial-renders-blank-state)
- [ ] T51: when a stock record has NULL batch_number and NULL serial_number, the remaining record fields still display correctly in the detail view at /stock/:id  (AC3-empty-batch-serial-renders-blank-state)
- [ ] T52: when stock records have NULL batch_number and NULL serial_number, the list view at /stock renders an element with class empty-state per affected record without a broken layout  (AC3-empty-batch-serial-renders-blank-state)
- [ ] T53: given a stock record with split batch and serial fields, when the detail API endpoint is called, the JSON response does not contain an inventory_code field  (AC4-combined-code-no-longer-displayed)
- [ ] T54: given stock records exist with split batch and serial fields, when the stock list API endpoint is called, no record in the JSON response contains an inventory_code field  (AC4-combined-code-no-longer-displayed)
- [ ] T55: the stock API JSON response for any stock item contains no combined inventory_code field; only the discrete batch_number and serial_number fields appear in the payload  (AC4-combined-code-no-longer-displayed)
- [ ] T56: navigating to /stock/:id in <App> finds no element displaying the combined tracking code (inventory_code) in the card  (AC4-combined-code-no-longer-displayed)
- [ ] T57: navigating to /stock in <App> finds no element displaying the combined tracking code (inventory_code) in the stock-table  (AC4-combined-code-no-longer-displayed)

## Deferred / skipped
- (none)
