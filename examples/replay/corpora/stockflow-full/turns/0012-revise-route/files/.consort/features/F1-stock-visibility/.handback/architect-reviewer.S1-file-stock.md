REVISE (reflection gate): blocking smell "reflect-testlist-defect": reflection gate: NFR-F1-validation-messages ('Validation errors name the offending field rather than a generic bad request. Owned at the API boundary.') has no covering test. T5 checks that the service raises a validation error before any DB write (a service-layer concern), but no test asserts that the API boundary HTTP response body names the offending field. A behavior or fitness test must POST an invalid payload (e.g. negative quantity, missing required field) to the real-branch endpoint and assert the response contains a field-named error message.

PRESERVE every architectural annotations + architecture.json item this story ALREADY has , they passed prior gates and MUST remain. This is an ADDITIVE revise: keep all existing files/entries intact (do not delete, rename, renumber, merge, or overwrite them) and ADD the specific coverage named above alongside them. The named coverage is REQUIRED: add the missing test(s)/criterion that assert the exact behavior described , do NOT omit it, weaken it, or defer it to an open question. If the stated behavior genuinely cannot be tested as written, name the concrete blocker; do not punt. After writing, the story's architectural annotations + architecture.json set = every prior item PLUS the added one(s).