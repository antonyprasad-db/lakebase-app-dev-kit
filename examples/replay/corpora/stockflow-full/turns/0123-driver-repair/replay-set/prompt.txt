REPAIR a driver-fixable regression in AC AC1-column-dropped (story S2-drop-combined-code). The honest-GREEN verify against the running app FAILED and it was diagnosed (by the Navigator, or deterministically by a gate such as contract-clean) as a genuine regression in the code, NOT a superseded test:
  DIAGNOSIS: No production code regression remains: app/models.py, app/repositories/stock.py, and app/routes/stock.py no longer reference inventory_code (the prior repair fixed them). The only remaining failures are prior tests from earlier stories that still seed/insert/assert inventory_code — these are superseded by AC1 which drops the column. Superseded files: tests/step_defs/test_S1_add_and_backfill_columns.py, tests/step_defs/test_S1_file_stock.py, tests/step_defs/test_S2_drop_combined_code.py, tests/step_defs/test_S3_view_sku_detail.py, tests/test_S1_add_backfill_migration_reversible.py, tests/test_stock_db_invariants.py
  FIX: Permissive-green refactor ONLY the six superseded test files listed in the diagnosis — remove all inventory_code from INSERT statements, seed fixtures, fixture yields, and assertions in each file. Do NOT modify any production code or any test that does not reference inventory_code.
Apply that fix to the PRODUCTION code. Do NOT edit prior tests to force this regression green, fix the code. (EXCEPTION: if a SUPERSEDED TESTS directive follows below, the Navigator flagged those specific prior tests as encoding obsolete behavior, refactor ONLY those alongside this fix , often the regression is collateral from a superseded test erroring on a shared session, so both must land in this one turn.) Keep the AC's own tests green. This is your ONE repair attempt: if the verify still fails after it, the orchestration escalates to a human with the diagnosis.

SUPERSEDED TESTS: this AC (AC1-column-dropped) supersedes behavior encoded in PRIOR tests the Navigator flagged (AC1 drops the inventory_code column; all prior tests that seed, query, or assert on inventory_code are superseded — including fitness, migration-reversibility, and schema-invariant tests that assert properties of the now-gone column). The latest AC wins. You MAY refactor ONLY these flagged tests to the new behavior (alongside the production code) so the honest-GREEN verify holds:
  - tests/step_defs/test_S1_add_and_backfill_columns.py
  - tests/step_defs/test_S1_file_stock.py
  - tests/step_defs/test_S2_drop_combined_code.py
  - tests/step_defs/test_S3_view_sku_detail.py
  - tests/test_S1_add_backfill_migration_reversible.py
  - tests/test_stock_db_invariants.py
Do NOT touch any other test; an UNflagged failing test is a genuine regression that must stay red and escalate.