Realize the physical database schema for story S1-add-and-backfill-columns into <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/db-design.json (+ a short db-design.md narrative). Story S1-add-and-backfill-columns's ACs are: AC1-columns-exist, AC2-backfill-parses-conforming-code, AC3-nonconforming-code-leaves-nulls, AC4-every-row-survives, AC5-integrity-probe-reports-nonconforming-count. Read architecture.json (service_backed, layers, persistence_invariants) , the architect owns that logical contract; you produce the PHYSICAL realization and do NOT re-author the invariants. Declare tables[] (columns with explicit type/nullable/default, primary_key, unique_constraints, foreign_keys, checks, indexes) and this story's schema_changes[] (the per-story migration plan the build lane authors the Alembic migration from; keep an expand/contract column split or drop reversible). Populate realizes_invariants[] as a flat array of the architecture.json persistence_invariant id STRINGS (bare ids, not objects) , an uncovered invariant hard-blocks the spec gate. This feature is service_backed. Mirror the architect's models package (app/models/), one table per domain object. Realize EVERY declared persistence_invariant and list its id in realizes_invariants[]: PI1-batch-serial-nullable [not_null on stock] (batch_number and serial_number are added as NULLABLE columns; nonconforming codes leave them NULL rather than violating a constraint (AC3). The NOT NULL invariant is that this constraint is DEFERRED for these two columns while sku/location/quantity remain NOT NULL.); PI2-sku-location-unique-preserved [unique on stock] (The composite UNIQUE(sku, location) from F1 survives the add-and-backfill migration unchanged; location stays the canonical addressing column and is not re-derived from the code leading segment (R3).); PI3-add-backfill-atomic [transactional on stock] (The column-add plus backfill runs as a single atomic migration transaction: either every row is backfilled or none is, so a failed run cannot half-populate or drop rows (AC4, R1).); PI4-every-row-preserved [check on stock] (Row count and each row's canonical sku/location/quantity are unchanged across the migration; the backfill is an in-place UPDATE that never DELETEs or mutates canonical fields, verified by a before/after diff on the paired branch (AC4, R1).); PI5-migration-reversible [migration_reversible on stock] (The Alembic upgrade adding batch_number/serial_number has a working downgrade; end-state reversibility for the split is provided by reconstructing inventory_code from canonical location plus batch/serial, not by leaving the column in place (R1).). Write every artifact under the ABSOLUTE artifact root <PROJECT_ROOT>/.consort (this feature: <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/; this story: <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/stories/S1-add-and-backfill-columns/); use that absolute path and never resolve or guess the project root yourself.