Realize the physical database schema for story S1-file-stock into <PROJECT_ROOT>/.consort/features/F1-stock-visibility/db-design.json (+ a short db-design.md narrative). Story S1-file-stock's ACs are: AC1-file-new-stock, AC2-record-carries-inventory-code, AC3-refile-same-pair-updates-in-place. Read architecture.json (service_backed, layers, persistence_invariants) , the architect owns that logical contract; you produce the PHYSICAL realization and do NOT re-author the invariants. Declare tables[] (columns with explicit type/nullable/default, primary_key, unique_constraints, foreign_keys, checks, indexes) and this story's schema_changes[] (the per-story migration plan the build lane authors the Alembic migration from; keep an expand/contract column split or drop reversible). Populate realizes_invariants[] as a flat array of the architecture.json persistence_invariant id STRINGS (bare ids, not objects) , an uncovered invariant hard-blocks the spec gate. This feature is service_backed. Mirror the architect's models package (app/models/), one table per domain object. Realize EVERY declared persistence_invariant and list its id in realizes_invariants[]: PI1-stock-sku-location-unique [unique on stock] (Composite unique constraint on (sku, location): no two rows share the same SKU/location pair (NFR R3).); PI2-stock-quantity-nonnegative [check on stock] (CHECK quantity >= 0: a stock quantity can never be persisted below zero (NFR R2).); PI3-stock-required-fields-not-null [not_null on stock] (sku, location, and quantity are NOT NULL; a stock record cannot exist without its identifying pair and quantity.); PI4-refile-upsert-atomic [transactional on stock] (A refile of an existing (sku, location) pair is a single atomic upsert; on collision the existing row is updated in one transaction, never inserted as a duplicate (NFR R3, AC3).); PI5-migration-reversible [migration_reversible on stock] (The Alembic migration creating the stock table has a working downgrade and preserves pre-existing inventory records on upgrade (NFR R1).). Write every artifact under the ABSOLUTE artifact root <PROJECT_ROOT>/.consort (this feature: <PROJECT_ROOT>/.consort/features/F1-stock-visibility/; this story: <PROJECT_ROOT>/.consort/features/F1-stock-visibility/stories/S1-file-stock/); use that absolute path and never resolve or guess the project root yourself.