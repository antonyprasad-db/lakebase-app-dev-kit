Write the failing tests (RED) for the WHOLE story S3-expose-batch-serial-in-stock-ui in this one turn, EXACTLY these 24 item(s) across all its ACs, in order: T34 [ac AC1-detail-view-shows-batch-and-serial]: "given a stock record with batch_number and serial_number populated, when the detail API endpoint is called for that record, then the JSON response contains batch_number as a distinct top-level field with its correct value"; T35 [ac AC1-detail-view-shows-batch-and-serial]: "given a stock record with batch_number and serial_number populated, when the detail API endpoint is called for that record, then the JSON response contains serial_number as a distinct top-level field with its correct value"; T36 [ac AC1-detail-view-shows-batch-and-serial]: "the stock detail API boundary returns batch_number and serial_number as distinct fields in a JSON response body with no server-rendered HTML (NFR-F6-spa-json-boundary)"; T37 [ac AC1-detail-view-shows-batch-and-serial]: "navigating to /stock/:id in <App> renders the detail view with batch number in a labeled field carrying the card design-guide class"; T38 [ac AC1-detail-view-shows-batch-and-serial]: "navigating to /stock/:id in <App> renders the detail view with serial number in a labeled field carrying the card design-guide class"; T39 [ac AC1-detail-view-shows-batch-and-serial]: "batch number and serial number appear as two distinct separately labeled fields (not merged into one element) in the detail view at /stock/:id"; T40 [ac AC2-list-view-shows-batch-and-serial]: "given stock records exist, when the stock list API endpoint is called, then each record in the JSON response contains batch_number as a distinct field"; T41 [ac AC2-list-view-shows-batch-and-serial]: "given stock records exist, when the stock list API endpoint is called, then each record in the JSON response contains serial_number as a distinct field"; T42 [ac AC2-list-view-shows-batch-and-serial]: "navigating to /stock in <App> renders the list view with each stock record showing a batch number field carrying the stock-table design-guide class"; T43 [ac AC2-list-view-shows-batch-and-serial]: "navigating to /stock in <App> renders the list view with each stock record showing a serial number field carrying the stock-table design-guide class"; T44 [ac AC2-list-view-shows-batch-and-serial]: "batch number and serial number appear as separate fields per row in the list view at /stock"; T45 [ac AC3-empty-batch-serial-renders-blank-state]: "given a stock record whose batch_number is NULL, when the detail API endpoint is called, then batch_number is present in the JSON response as null rather than absent or causing an error"; T46 [ac AC3-empty-batch-serial-renders-blank-state]: "given a stock record whose serial_number is NULL, when the detail API endpoint is called, then serial_number is present in the JSON response as null rather than absent or causing an error"; T47 [ac AC3-empty-batch-serial-renders-blank-state]: "given a stock record whose batch_number and serial_number are NULL, when the stock list API endpoint is called, then batch_number and serial_number appear as null in the JSON response and the remainder of the record fields are still present"; T48 [ac AC3-empty-batch-serial-renders-blank-state]: "the stock detail API returns batch_number as JSON null (not omitted, not an error) when the column value is NULL on the real branch database"; T49 [ac AC3-empty-batch-serial-renders-blank-state]: "when a stock record has NULL batch_number, the detail view at /stock/:id renders an element with class empty-state instead of an error or broken layout"; T50 [ac AC3-empty-batch-serial-renders-blank-state]: "when a stock record has NULL serial_number, the detail view at /stock/:id renders an element with class empty-state instead of an error or broken layout"; T51 [ac AC3-empty-batch-serial-renders-blank-state]: "when a stock record has NULL batch_number and NULL serial_number, the remaining record fields still display correctly in the detail view at /stock/:id"; T52 [ac AC3-empty-batch-serial-renders-blank-state]: "when stock records have NULL batch_number and NULL serial_number, the list view at /stock renders an element with class empty-state per affected record without a broken layout"; T53 [ac AC4-combined-code-no-longer-displayed]: "given a stock record with split batch and serial fields, when the detail API endpoint is called, the JSON response does not contain an inventory_code field"; T54 [ac AC4-combined-code-no-longer-displayed]: "given stock records exist with split batch and serial fields, when the stock list API endpoint is called, no record in the JSON response contains an inventory_code field"; T55 [ac AC4-combined-code-no-longer-displayed]: "the stock API JSON response for any stock item contains no combined inventory_code field; only the discrete batch_number and serial_number fields appear in the payload"; T56 [ac AC4-combined-code-no-longer-displayed]: "navigating to /stock/:id in <App> finds no element displaying the combined tracking code (inventory_code) in the card"; T57 [ac AC4-combined-code-no-longer-displayed]: "navigating to /stock in <App> finds no element displaying the combined tracking code (inventory_code) in the stock-table". Write ALL of them now and ONLY these; do NOT add or drop items, the orchestration stamps ONE whole-story batch RED cycle for exactly these ids, and any mismatch is a defect. UI track is ON: the UI must adhere to the project design guide at <PROJECT_ROOT>/.consort/design/design-guide.md (+ the design-guide.json tokens). Build to it. RUBRIC (pre-extracted; judge against THIS) :: layer=E2E | required NFRs, NFR-F6-data-durability (Every sprint-1 stock row survives the schema refactor with no loss or corruption. The add-and-backfill migration is additive and atomic (PI3, PI4); nonconforming codes are left NULL rather than guessed or dropped (AC3), and an integrity probe surfaces the nonconforming count for review before acceptance (AC5). Reversibility is provided by a paired downgrade (PI5).); NFR-F6-unique-identity-preserved (Splitting the combined code into batch_number and serial_number gives each stock position a single unambiguous identity with separately addressable fields (R3). The canonical (sku, location) uniqueness from F1 is preserved unchanged (PI2); location is NOT re-derived from the code's leading segment.); NFR-F6-real-branch-integration-tests (Correctness is proven by a parent-aware schema diff plus the integrity-probe count run against the paired Lakebase branch via DATABASE_URL — real integration tests (pytest-bdd, Alembic applied to the branch first), never a mock or in-memory substitute. CI refuses to merge if integration tests do not run against a real branch.); NFR-F6-spa-json-boundary (Where the combined code was shown before, batch and serial are exposed as distinct fields in the React + TypeScript SPA under client/, fed by the JSON API boundary (renders_via: react, no server-rendered HTML). Applies to the UI story S3; S1 (this story) is a schema/migration change with no boundary surface.); NFR-F6-clean-render-optional-detail (A stock row whose batch_number/serial_number are NULL (nonconforming legacy code) still renders cleanly: batch and serial default to an explicit 'not tracked' rather than a null crash or a blank region (nfrs.md clean-render preference). Owned in the React SPA view; the boundary returns null fields as JSON.); NFR-F6-no-overcommit-na (N/A - this feature is a schema refactor of the tracking-code columns and does not touch quantity write paths, picks, or allocation. The non-negative/no-overcommit floor from F1 (PI2 on quantity) is unaffected and carried unchanged.) | design-token groups, typography, colors, spacing, radius, shadows, breakpoints, components. The rubric above is pre-extracted from <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/architecture.md, <PROJECT_ROOT>/.consort/nfrs.md, and <PROJECT_ROOT>/.consort/design/design-guide.md, open those full files ONLY if you need more detail than it carries (do not re-read them by default). LAYOUT (place/judge code at THESE paths, do not scan for them) :: boundary=app/routes (react) | service=app/services | repository=app/repositories | models=app/models.