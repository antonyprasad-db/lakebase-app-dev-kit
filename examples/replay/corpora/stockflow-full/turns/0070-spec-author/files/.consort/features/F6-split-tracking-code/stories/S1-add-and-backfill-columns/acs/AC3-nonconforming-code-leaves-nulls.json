{
  "id": "AC3-nonconforming-code-leaves-nulls",
  "given": "a stock row whose inventory_code has no batch or serial segment, such as \"X-1\" or a bare \"c\"",
  "when": "the add-and-backfill migration is applied",
  "then": "batch_number and serial_number are left NULL rather than being guessed or dropped, and the row is preserved unchanged otherwise",
  "status": "draft",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC2 covers the conforming-code parse path; this covers the nonconforming-code path, which is independently breakable — a build could parse full codes correctly yet crash, guess, or drop on variable-width codes that lack segments."
  }
}
