{
  "id": "AC5-integrity-probe-reports-nonconforming-count",
  "given": "a stock table containing a mix of conforming and nonconforming inventory_code values after backfill",
  "when": "the integrity probe is run",
  "then": "it surfaces the count of rows that did not parse as location-batch-serial (those left with NULL batch_number and serial_number) for review before the change is accepted",
  "status": "draft",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC3 asserts nonconforming rows are left NULL; this asserts a separate observable output — a reviewable count of those rows. A build could correctly NULL nonconforming rows yet never report how many there were."
  }
}
