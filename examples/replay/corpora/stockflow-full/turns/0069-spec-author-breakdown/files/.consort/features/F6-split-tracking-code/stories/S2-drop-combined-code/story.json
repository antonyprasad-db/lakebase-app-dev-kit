{
  "id": "S2-drop-combined-code",
  "scope": "Drop the combined inventory_code column and provide reversible down-migration",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "Dropping the column is independently observable and testable from backfilling. The code can add and backfill batch/serial while inventory_code remains present (confirmed via schema diff), so the drop is a distinct outcome that can be reversed independently via the down-migration."
  }
}
