Write the failing tests (RED) for the WHOLE story S3-view-sku-detail in this one turn, EXACTLY these 13 item(s) across all its ACs, in order: T31 [ac AC1-detail-lists-sku-locations]: "GET /stock/<sku> returns a JSON array containing every location where that SKU holds stock, each entry carrying the correct quantity"; T32 [ac AC1-detail-lists-sku-locations]: "GET /stock/<sku> returns no entries whose location belongs to a different SKU that was seeded in the same test run"; T33 [ac AC1-detail-lists-sku-locations]: "rendering <App> in MemoryRouter at '/sku/SKU-A' shows the SKU detail page in the document"; T34 [ac AC1-detail-lists-sku-locations]: "when the API returns stock records for SKU-A at two locations, the detail view renders one row per location for that SKU"; T35 [ac AC1-detail-lists-sku-locations]: "each location row carries a data-testid containing the location name returned by the API"; T36 [ac AC1-detail-lists-sku-locations]: "each location row carries a data-testid containing the quantity returned by the API for that location"; T37 [ac AC2-detail-shows-tracking-code]: "GET /stock/<sku> returns the combined inventory tracking code for each location exactly as it was filed via POST /stock"; T38 [ac AC2-detail-shows-tracking-code]: "each location row carries a data-testid containing the combined tracking code exactly as returned by the API"; T39 [ac AC3-par-level-not-tracked]: "GET /stock/<sku> returns HTTP 200 with par_level equal to null when no par level was recorded for that stock record"; T40 [ac AC3-par-level-not-tracked]: "rendering <App> in MemoryRouter at '/sku/SKU-A' shows the par-level section element in the document"; T41 [ac AC3-par-level-not-tracked]: "when the API returns a stock record with par_level null, the detail view displays a 'not tracked' indication rather than a blank or null value"; T42 [ac AC3-par-level-not-tracked]: "the 'not tracked' par-level element carries a data-testid so the seam is assertable without relying on inline styles"; T43 [ac AC3-par-level-not-tracked]: "the stock service detail method returns par_level as Python None (not omitted from the result dict, not raised as an exception) when no par_level is recorded for the queried SKU/location". Write ALL of them now and ONLY these; do NOT add or drop items, the orchestration stamps ONE whole-story batch RED cycle for exactly these ids, and any mismatch is a defect. UI track is ON: the UI must adhere to the project design guide at <PROJECT_ROOT>/.consort/design/design-guide.md (+ the design-guide.json tokens). Build to it. RUBRIC (pre-extracted; judge against THIS) :: layer=API | required NFRs, NFR-F1-data-durability (Existing stock data survives every schema migration with no loss; model changes are additive and pre-existing inventory records are preserved (PI5). Adjustment audit fields (timestamp, actor) are immutable once written.); NFR-F1-no-negative-overcommit (Stock levels never go below zero and never overcommit; enforced at write time in the service layer and by a DB CHECK constraint (PI2). Note: the pick/overcommit flow is out of S1 scope but the non-negative floor is realized here.); NFR-F1-real-branch-integration-tests (Acceptance tests are real integration tests (pytest-bdd) against the paired Lakebase branch via DATABASE_URL, never a mock or in-memory substitute; CI refuses to merge if integration tests do not run against a real branch.); NFR-F1-spa-json-boundary (The UI is a React + TypeScript single-page app under client/; the API boundary returns JSON (not server-rendered HTML), so the boundary layer declares renders_via: react. Client-side navigation with no full-page reloads; adjustments update the affected row in place.); NFR-F1-sku-detail-clean-render (The SKU-detail read view renders cleanly regardless of optional-field presence: it lists every location/quantity for the SKU and shows the tracking code exactly as filed; a missing par level (or other optional detail) shows an explicit 'not tracked' rather than a blank region, a null value, or an error (nfrs.md clean-render preference, S3-AC3). The read endpoint emits a clean JSON collection with nulls preserved for unrecorded optional fields (never a 4xx/5xx), owned at the boundary; the 'not tracked' presentation is a view responsibility in the React SPA (client/).). The rubric above is pre-extracted from <PROJECT_ROOT>/.consort/features/F1-stock-visibility/architecture.md, <PROJECT_ROOT>/.consort/nfrs.md, and <PROJECT_ROOT>/.consort/design/design-guide.md, open those full files ONLY if you need more detail than it carries (do not re-read them by default). LAYOUT (place/judge code at THESE paths, do not scan for them) :: boundary=app/routes (react) | service=app/services | repository=app/repositories | models=app/models.