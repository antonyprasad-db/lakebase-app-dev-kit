Write the failing tests (RED) for the WHOLE story S1-file-stock in this one turn, EXACTLY these 19 item(s) across all its ACs, in order: T1 [ac AC1-file-new-stock]: "The routes module (app/routes/) contains no import of SQLAlchemy Session or any ORM construct"; T2 [ac AC1-file-new-stock]: "The services module (app/services/) contains no import of SQLAlchemy Session or any ORM construct"; T3 [ac AC1-file-new-stock]: "DATABASE_URL is read from the environment variable and is not hardcoded anywhere in the application source"; T4 [ac AC1-file-new-stock]: "POST a new SKU/location/quantity creates a stock record and GET returns the same SKU, location, and quantity"; T5 [ac AC1-file-new-stock]: "POST a payload with a negative quantity returns an HTTP error response body that names the offending field"; T6 [ac AC1-file-new-stock]: "POST a payload missing the required sku field returns an HTTP error response body that names the offending field"; T7 [ac AC1-file-new-stock]: "Navigating <App> to the file-stock route renders the file-stock form with a data-testid='sku-input' element"; T8 [ac AC1-file-new-stock]: "Navigating <App> to the file-stock route renders the file-stock form with a data-testid='location-input' element"; T9 [ac AC1-file-new-stock]: "Navigating <App> to the file-stock route renders the file-stock form with a data-testid='quantity-input' element"; T10 [ac AC1-file-new-stock]: "Navigating <App> to the file-stock route renders the file-stock form with a data-testid='file-stock-submit' submit control"; T11 [ac AC1-file-new-stock]: "The stock service rejects a write with a negative quantity value before any database write occurs"; T12 [ac AC1-file-new-stock]: "Inserting two rows with the same (sku, location) pair into the stock table raises a unique-constraint violation on the real branch database"; T13 [ac AC1-file-new-stock]: "Inserting a row with quantity below zero into the stock table raises a check-constraint violation on the real branch database"; T14 [ac AC1-file-new-stock]: "Inserting a row with a NULL sku into the stock table raises a not-null-constraint violation on the real branch database"; T15 [ac AC1-file-new-stock]: "Running the stock table migration downgrade followed immediately by upgrade recreates the stock table with all required columns and constraints on the real branch database"; T16 [ac AC2-record-carries-inventory-code]: "GET a stock record filed with a combined inventory_code returns the inventory_code exactly as filed"; T17 [ac AC2-record-carries-inventory-code]: "Navigating <App> to the file-stock route renders the file-stock form with a data-testid='inventory-code-input' element"; T18 [ac AC3-refile-same-pair-updates-in-place]: "POST the same SKU/location with a different quantity updates the existing record in place and GET returns exactly one record with the new quantity"; T19 [ac AC3-refile-same-pair-updates-in-place]: "A refile of an existing (sku, location) pair results in exactly one row in the stock table — no duplicate is inserted — verified on the real branch database". Write ALL of them now and ONLY these; do NOT add or drop items, the orchestration stamps ONE whole-story batch RED cycle for exactly these ids, and any mismatch is a defect. UI track is ON: the UI must adhere to the project design guide at <PROJECT_ROOT>/.consort/design/design-guide.md (+ the design-guide.json tokens). Build to it. RUBRIC (pre-extracted; judge against THIS) :: layer=API | required NFRs, NFR-F1-data-durability (Existing stock data survives every schema migration with no loss; model changes are additive and pre-existing inventory records are preserved (PI5). Adjustment audit fields (timestamp, actor) are immutable once written.); NFR-F1-no-negative-overcommit (Stock levels never go below zero and never overcommit; enforced at write time in the service layer and by a DB CHECK constraint (PI2). Note: the pick/overcommit flow is out of S1 scope but the non-negative floor is realized here.); NFR-F1-sku-location-uniqueness (Every (sku, location) pair is uniquely addressable; a collision is resolved at write time via atomic upsert (PI1, PI4), never stored as a duplicate and never surfaced as an error page (AC3).); NFR-F1-real-branch-integration-tests (Acceptance tests are real integration tests (pytest-bdd) against the paired Lakebase branch via DATABASE_URL, never a mock or in-memory substitute; CI refuses to merge if integration tests do not run against a real branch.); NFR-F1-spa-json-boundary (The UI is a React + TypeScript single-page app under client/; the API boundary returns JSON (not server-rendered HTML), so the boundary layer declares renders_via: react. Client-side navigation with no full-page reloads; adjustments update the affected row in place.); NFR-F1-validation-messages (Validation errors name the offending field rather than a generic 'bad request'. Owned at the API boundary (PO preference). REQUIRED coverage: a real-branch behavior/fitness test must POST an invalid file payload (e.g. negative quantity, missing required field) to the boundary endpoint and assert the HTTP error response body NAMES the offending field. This is a boundary-response contract distinct from the service-layer 'validation before any DB write' check — the service test does not exercise the HTTP response shape and does not satisfy this NFR.). The rubric above is pre-extracted from <PROJECT_ROOT>/.consort/features/F1-stock-visibility/architecture.md, <PROJECT_ROOT>/.consort/nfrs.md, and <PROJECT_ROOT>/.consort/design/design-guide.md, open those full files ONLY if you need more detail than it carries (do not re-read them by default). LAYOUT (place/judge code at THESE paths, do not scan for them) :: boundary=app/routes (react) | service=app/services | repository=app/repositories | models=app/models.