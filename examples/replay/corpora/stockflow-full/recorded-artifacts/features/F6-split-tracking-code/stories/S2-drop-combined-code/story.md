# S2-drop-combined-code

As a warehouse data engineer,  
I want the inventory_code column to be dropped from the schema after backfill,  
So that the end-state schema carries only the split batch and serial columns.

## Acceptance criteria

See `acs/` folder.
