Write the failing tests (RED) for the WHOLE story S3-display-batch-serial-separately in this one turn, EXACTLY these 19 item(s) across all its ACs, in order: T28 [ac AC1-stock-list-shows-separate-fields]: "The stock-api boundary module (app/routes/) does not import SQLAlchemy Session or any ORM session object — all data reads in the display path flow through the service layer"; T29 [ac AC1-stock-list-shows-separate-fields]: "The stock-service module (app/services/) contains no SQLAlchemy Session or ORM imports — it delegates all persistence reads to app/repositories/ and holds no session references"; T30 [ac AC1-stock-list-shows-separate-fields]: "The database connection URL used by the stock display path is resolved exclusively from the DATABASE_URL environment variable — no connection string is hardcoded in any module touched by this story"; T32 [ac AC1-stock-list-shows-separate-fields]: "Given a stock record seeded with a uuid-suffixed sku and batch_number 'B001', when GET /api/stock returns the list, then the response contains a row with batch_number field equal to 'B001'"; T33 [ac AC1-stock-list-shows-separate-fields]: "Given a stock record seeded with a uuid-suffixed sku and serial_number 'S001', when GET /api/stock returns the list, then the response contains a row with serial_number field equal to 'S001'"; T34 [ac AC1-stock-list-shows-separate-fields]: "rendering <App> in MemoryRouter at /stock with a mocked API response containing batch_number shows a list row with a data-testid='batch-number' element displaying the batch_number value"; T35 [ac AC1-stock-list-shows-separate-fields]: "rendering <App> in MemoryRouter at /stock with a mocked API response containing serial_number shows a list row with a data-testid='serial-number' element displaying the serial_number value"; T31 [ac AC4-combined-code-not-displayed]: "fetching the stock list returns a response with Content-Type application/json and a JSON body — the boundary does not return server-rendered HTML"; T44 [ac AC4-combined-code-not-displayed]: "Given any stock record in the system, when GET /api/stock returns the list, then no record in the response contains an inventory_code field"; T45 [ac AC4-combined-code-not-displayed]: "rendering <App> in MemoryRouter at /stock with a mocked API response shows no element with text or data-testid referencing inventory_code in the list view"; T46 [ac AC4-combined-code-not-displayed]: "rendering <App> in MemoryRouter at /stock/new shows no element with text or data-testid referencing inventory_code in the form view"; T36 [ac AC2-stock-form-has-separate-inputs]: "Given a POST /api/stock request with separate batch_number and serial_number fields using a uuid-suffixed sku, the record is persisted with the submitted batch_number value"; T37 [ac AC2-stock-form-has-separate-inputs]: "Given a POST /api/stock request with separate batch_number and serial_number fields using a uuid-suffixed sku, the record is persisted with the submitted serial_number value"; T38 [ac AC2-stock-form-has-separate-inputs]: "rendering <App> in MemoryRouter at /stock/new shows a form with a data-testid='batch-number-input' labeled input field for batch_number"; T39 [ac AC2-stock-form-has-separate-inputs]: "rendering <App> in MemoryRouter at /stock/new shows a form with a data-testid='serial-number-input' labeled input field for serial_number"; T40 [ac AC3-untracked-values-shown-clearly]: "Given a stock record seeded with a uuid-suffixed sku and batch_number NULL, when GET /api/stock returns the list, then the response contains a row where batch_number is explicitly null"; T41 [ac AC3-untracked-values-shown-clearly]: "Given a stock record seeded with a uuid-suffixed sku and serial_number NULL, when GET /api/stock returns the list, then the response contains a row where serial_number is explicitly null"; T42 [ac AC3-untracked-values-shown-clearly]: "rendering <App> in MemoryRouter at /stock with a mocked API response where batch_number is null shows a data-testid='batch-number' element with a 'not tracked' indicator rather than a blank or reconstructed value"; T43 [ac AC3-untracked-values-shown-clearly]: "rendering <App> in MemoryRouter at /stock with a mocked API response where serial_number is null shows a data-testid='serial-number' element with a 'not tracked' indicator rather than a blank or reconstructed value". Write ALL of them now and ONLY these; do NOT add or drop items, the orchestration stamps ONE whole-story batch RED cycle for exactly these ids, and any mismatch is a defect. TEST STATE OWNERSHIP (mandatory): each test OWNS the state it asserts on , the acceptance DB is a shared branch reused across the story's cycles and already holds rows other stories committed. For any COLLECTION or AGGREGATE assertion (an empty list/table, "returns all", a count), NEVER assume ambient state and NEVER assert absolute whole-table state (e.g. len(all) == 0): SCOPE to per-run-unique keys , assert only your own seeded rows, or query a per-run-unique slice that is genuinely empty , or explicitly clear the aggregate you claim empty. Also revert/roll back rows a test writes so it never leaks state into another test. UI track is ON: the UI must adhere to the project design guide at <PROJECT_ROOT>/.consort/design/design-guide.md (+ the design-guide.json tokens). Build to it. RUBRIC (pre-extracted; judge against THIS) :: layer=E2E | required NFRs, NFR-F6-2-no-negative-stock (N/A for this feature — the split-tracking refactor changes only the stock record's schema and display, not quantity math or the write path. The no-overcommit / non-negative guarantee (CHECK + service guard) established in F1 is preserved unchanged; this feature introduces no code that could drive quantity negative.); NFR-F6-4-real-branch-integration-tests (Every AC in this feature is verified by integration tests running against the paired Lakebase branch (databricks_postgres via DATABASE_URL), never a mock or in-memory substitute; migrations are applied to the branch first. CI refuses a PR whose integration tests do not run against a real branch.); NFR-F6-5-spa-json-api (The boundary stays a JSON API returning data (not server-rendered HTML); the React + TypeScript SPA under client/ renders batch_number and serial_number as separate fields with client-side navigation and in-place updates. Realized in S2 (API) and S3 (UI); the API paths are covered by the R4 branch integration tests and the UI by client component tests.); NFR-F6-8-untracked-batch-serial-clean-render (A stock row whose batch_number/serial_number are NULL (non-conforming legacy code) renders cleanly in the SPA: batch and serial default to an explicit 'not tracked' indication, never a null crash or a blank region. Owned by the client presentation layer over the JSON boundary; covered by a SPA component test on the untracked path plus the R4 branch integration test. (PO preference.)) | design-token groups, typography, colors, spacing, radius, shadows, breakpoints, components. The rubric above is pre-extracted from <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/architecture.md, <PROJECT_ROOT>/.consort/nfrs.md, and <PROJECT_ROOT>/.consort/design/design-guide.md, open those full files ONLY if you need more detail than it carries (do not re-read them by default). LAYOUT (place/judge code at THESE paths, do not scan for them) :: boundary=app/routes | service=app/services | repository=app/repositories | models=app/models.