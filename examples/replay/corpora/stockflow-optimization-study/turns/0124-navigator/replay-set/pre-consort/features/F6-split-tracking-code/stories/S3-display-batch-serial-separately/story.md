# S3: Display batch and serial separately in UI

As a warehouse operator,
I want to see batch_number and serial_number as separate fields in the stock list and forms,
so that I can view and manage batch and serial information independently.

## Scope

Update the UI (stock list view, detail pages, forms) to display batch_number and serial_number as distinct, separately visible fields wherever the combined inventory_code was previously shown. Ensure the user sees these fields as separate, editable (if applicable), and independently understandable.

Layer: E2E (UI)
