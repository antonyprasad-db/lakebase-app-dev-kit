REPAIR a driver-fixable regression in AC AC1-stock-list-shows-separate-fields (story S3-display-batch-serial-separately). The honest-GREEN verify against the running app FAILED and it was diagnosed (by the Navigator, or deterministically by a gate such as contract-clean) as a genuine regression in the code, NOT a superseded test:
  DIAGNOSIS: StockPage T42/T43 fail because rendering /stock produces only the navbar: the client /stock stock-list surface is not built for the new fields. App.tsx has no /stock route and there is no StockPage component, and client/src/api/stock.ts StockRecord still only declares sku/location/quantity (no batch_number/serial_number). So data-testid='batch-number' and data-testid='serial-number' never appear, and the required 'not tracked' indicator for a null batch_number/serial_number is absent. This is the current AC's new behavior, not superseded.
  FIX: Add batch_number: string|null and serial_number: string|null to the StockRecord type in client/src/api/stock.ts. Build (or complete) the /stock StockPage list, wire it into App.tsx <Routes> plus a nav affordance per the IA, and for each record render its batch_number in data-testid='batch-number' and serial_number in data-testid='serial-number', substituting a 'not tracked' indicator (never blank, never omit the element) when the value is null. Style via the design-guide component vocabulary/tokens.
Apply that fix to the PRODUCTION code. Do NOT edit prior tests to force this regression green, fix the code. (EXCEPTION: if a SUPERSEDED TESTS directive follows below, the Navigator flagged those specific prior tests as encoding obsolete behavior, refactor ONLY those alongside this fix , often the regression is collateral from a superseded test erroring on a shared session, so both must land in this one turn.) Keep the AC's own tests green. This is your ONE repair attempt: if the verify still fails after it, the orchestration escalates to a human with the diagnosis.

SUPERSEDED TESTS: this AC (AC1-stock-list-shows-separate-fields) supersedes behavior encoded in PRIOR tests the Navigator flagged (S3/AC1 completes the F6 split: the migration dropped inventory_code and replaced it with separate batch_number + serial_number fields. All prior tests asserting inventory_code (its presence, segment derivation, downgrade reconstruction, and API/list absence) assert retired behavior superseded by the split; the Driver may permissively refactor only those inventory_code assertions to carry the requirement forward to batch_number/serial_number.). The latest AC wins. You MAY refactor ONLY these flagged tests to the new behavior (alongside the production code) so the honest-GREEN verify holds:
  - tests/architecture/test_S1_migration.py
  - tests/step_defs/test_S1_perform_batch_serial_schema_migration.py
  - tests/step_defs/test_S2_expose_batch_serial_in_api.py
  - tests/step_defs/test_S3_display_batch_serial_separately.py
Do NOT touch any other test; an UNflagged failing test is a genuine regression that must stay red and escalate.