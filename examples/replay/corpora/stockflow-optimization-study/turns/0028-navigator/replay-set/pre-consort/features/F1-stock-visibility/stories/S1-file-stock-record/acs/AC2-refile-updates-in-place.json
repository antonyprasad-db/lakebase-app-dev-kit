{
  "id": "AC2-refile-updates-in-place",
  "status": "draft",
  "given": "a stock record already exists for a (SKU, location) pair",
  "when": "a warehouse operator files a stock record for that same SKU-at-location pair again with a different quantity",
  "then": "the existing record is updated in place, exactly one record exists for that pair, no duplicate record is created, and no error page is shown",
  "layer": "API",
  "architectural_notes": "API boundary: the re-filing POST enters app/routes/ (boundary) -> app/services/ (business logic) -> app/repositories/ (the only layer touching the ORM/session) against stock_records via app/models/. The write-collision path (same (sku,location) filed twice) is resolved to an in-place update in the repository, guaranteed single-row by PI1 (unique composite key on (sku,location), brief_ref R3); no duplicate is inserted and no error page surfaces. No business logic lives in the boundary. Verified through the real-branch integration suite (NFR-F1-4).",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "This asserts the write-collision path (same pair filed twice resolves to an in-place update with no duplicate), which is independently breakable from AC1's first-write create: an implementation can create the first record correctly yet insert a duplicate or error on the second filing."
  }
}
