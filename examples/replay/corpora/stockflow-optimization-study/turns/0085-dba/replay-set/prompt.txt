Realize the physical database schema for story S1-perform-batch-serial-schema-migration into <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/db-design.json (+ a short db-design.md narrative). Story S1-perform-batch-serial-schema-migration's ACs are: AC1-backfill-wellformed-code, AC2-malformed-code-nulls, AC3-all-rows-survive, AC4-integrity-probe-count, AC5-inventory-code-dropped, AC6-down-migration-reconstructs. Read architecture.json (service_backed, layers, persistence_invariants) , the architect owns that logical contract; you produce the PHYSICAL realization and do NOT re-author the invariants. Declare tables[] (columns with explicit type/nullable/default, primary_key, unique_constraints, foreign_keys, checks, indexes) and this story's schema_changes[] (the per-story migration plan the build lane authors the Alembic migration from; keep an expand/contract column split or drop reversible). Populate realizes_invariants[] as a flat array of the architecture.json persistence_invariant id STRINGS (bare ids, not objects) , an uncovered invariant hard-blocks the spec gate. This feature is service_backed. Mirror the architect's models package (app/models/), one table per domain object. Realize EVERY declared persistence_invariant and list its id in realizes_invariants[]: PI1-migration-atomic [transactional on stock_records] (The up migration (add batch_number/serial_number, backfill, drop inventory_code) runs as a single atomic unit: every pre-existing row survives with unchanged identity and canonical location, row count is preserved, or the whole migration rolls back (R1, AC3).); PI2-batch-serial-nullable [check on stock_records] (batch_number and serial_number are NULLABLE so a non-conforming inventory_code backfills to NULL rather than a guessed value or a dropped row; the migration must tolerate short/malformed codes without raising (AC2).); PI3-migration-reversible [migration_reversible on stock_records] (The Alembic migration is reversible: downgrade re-adds inventory_code and reconstructs it from canonical location + batch_number + serial_number, dropping the new columns and restoring the pre-migration schema shape; a full up-then-down round trip is clean (AC6).); PI4-unique-sku-location-preserved [unique on stock_records] (The UNIQUE(sku, location) composite key is untouched by the refactor: location stays canonical and no new column replaces it, so every (sku, location) pair remains uniquely addressable across the migration (R3).). Write every artifact under the ABSOLUTE artifact root <PROJECT_ROOT>/.consort (this feature: <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/; this story: <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/stories/S1-perform-batch-serial-schema-migration/); use that absolute path and never resolve or guess the project root yourself.