Draft the acceptance criteria for story S1-perform-batch-serial-schema-migration and NOTHING else. The story: As a system, I want to the stock table schema to be refactored with batch_number and serial_number as first-class columns, so that batch and serial are stored separately and independently addressable. Write ONE file per AC as acs/<AC>.json (+ optional acs/<AC>.md), and put NOTHING else in acs/ (no test lists, no -tests.json / -test-list.json, no scratch files, the spec gate validates every acs/*.json against the AC schema and rejects non-AC files). The AC id MUST match AC<n>-<slug>: AC1-create-form, AC2-form-accepts-input, ... (an "AC" prefix + a number, then a kebab slug). A bare slug id like "create-form-displays" FAILS the schema and hard-blocks the spec gate. The file's "id" field MUST equal its basename (acs/AC1-foo.json has {"id":"AC1-foo"}). Write only under story S1-perform-batch-serial-schema-migration's acs/ directory. Do not create, draft, or modify acceptance criteria for any other story in this feature, each other story is drafted in its own separate step that you are not performing now, and you will be invoked again, once per story, for the rest. Authoring more than S1-perform-batch-serial-schema-migration here delays S1-perform-batch-serial-schema-migration reaching its spec gate and build, and is rejected at the gate. Write every artifact under the ABSOLUTE artifact root <PROJECT_ROOT>/.consort (this feature: <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/; this story: <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/stories/S1-perform-batch-serial-schema-migration/); use that absolute path and never resolve or guess the project root yourself.