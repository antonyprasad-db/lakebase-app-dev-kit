{
  "id": "AC3-untracked-values-shown-clearly",
  "status": "passing",
  "given": "a warehouse operator viewing a stock row whose batch_number and serial_number are null (a code that did not parse during the migration)",
  "when": "the row renders in the stock list or detail",
  "then": "each field shows a clear empty / not-tracked indication rather than a blank guessed value or a reconstructed combined code",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC1 and AC2 cover rows/forms with populated values; this AC covers the null render. A UI that displays populated batch/serial correctly could still render nulls as blank or a stray combined code, passing the prior ACs but failing here, so the null-state experience is independently breakable."
  },
  "layer": "E2E",
  "architectural_notes": "E2E: exercises the SPA null-path rendering of a persisted stock_records row whose batch_number/serial_number are NULL (PI2 tolerates non-conforming legacy codes) served verbatim through the JSON boundary, service, and repository. The 'not tracked' presentation defaulting is owned by the client presentation layer (NFR-F6-8) over the data boundary; the API returns null, the SPA never guesses or reconstructs a combined code, and no business logic infers values. Owner module is the SPA stock-list/detail component's empty-state rendering."
}
