REVISE (reflection gate): blocking smell "reflect-testlist-defect": reflection gate: T32 (kind:behavior, AC3) asserts 'GET the detail endpoint … returns an explicit not-tracked indicator rather than null or a blank value in the par level field', but AC3 architectural_notes and NFR-F1-8 both state the boundary returns null/omitted and the SPA client layer is responsible for rendering the explicit indicator. The R4 branch integration test for AC3 should assert the boundary returns the record with the par level field absent/null, not that the boundary itself emits a 'not-tracked indicator'. As written T32 pushes the presentation concern into the boundary, contradicting the architecture's SPA-owns-rendering assignment.

PRESERVE every ordered test list item this story ALREADY has , they passed prior gates and MUST remain. This is an ADDITIVE revise: keep all existing files/entries intact (do not delete, rename, renumber, merge, or overwrite them) and ADD the specific coverage named above alongside them. The named coverage is REQUIRED: add the missing test(s)/criterion that assert the exact behavior described , do NOT omit it, weaken it, or defer it to an open question. If the stated behavior genuinely cannot be tested as written, name the concrete blocker; do not punt. After writing, the story's ordered test list set = every prior item PLUS the added one(s).