{
  "id": "AC2-response-includes-serial-number",
  "status": "draft",
  "given": "a stock record whose serial_number holds a value",
  "when": "an API client fetches that stock record",
  "then": "the response contains a distinct serial_number field carrying that record's serial value",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC1 covers exposing batch_number; this AC covers serial_number. A serializer that maps batch but not serial makes AC1 green while this fails, and vice versa, so the two field exposures are independently breakable."
  }
}
