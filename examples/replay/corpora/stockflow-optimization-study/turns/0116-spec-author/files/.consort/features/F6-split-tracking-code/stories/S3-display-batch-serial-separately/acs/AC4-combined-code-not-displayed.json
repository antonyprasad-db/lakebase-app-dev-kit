{
  "id": "AC4-combined-code-not-displayed",
  "status": "draft",
  "given": "a warehouse operator using the updated UI wherever the combined inventory_code was previously shown",
  "when": "they view stock in the list, detail, or form",
  "then": "no combined inventory_code string is displayed anywhere; only the separate batch_number and serial_number fields appear in its place",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC1-AC3 assert the separate batch/serial fields are present and correctly rendered; this AC asserts the old combined code is gone from the UI. A build could add the new fields while still showing the legacy inventory_code alongside them, passing the prior ACs but failing this one, so its removal is independently observable and breakable."
  }
}
