Propose the sprint's candidate features for planning. WRITE the proposal to <PROJECT_ROOT>/.consort/planning/feature-proposals.md , author it FRESH from <PROJECT_ROOT>/.consort/product-overview.md + <PROJECT_ROOT>/.consort/nfrs.md (do NOT assume one already exists), one candidate feature per section, so the Architect can size them and the Product Owner can commit the backlog. UI track is ON: this product has a user-facing UI (a design-brief.md is part of intake), so every user-facing capability must be deliverable end to end as an E2E story, a real browser/screen interaction a user performs, not merely an API. Frame each candidate as a user-facing increment and note which need an E2E (UI) story.