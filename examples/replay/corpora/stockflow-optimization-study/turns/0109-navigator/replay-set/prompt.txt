Write the failing tests (RED) for the WHOLE story S2-expose-batch-serial-in-api in this one turn, EXACTLY these 10 item(s) across all its ACs, in order: T18 [ac AC1-response-includes-batch-number]: "The stock API route handler (app/routes/) does not import or reference the database session object directly — all data access flows through the service layer"; T19 [ac AC1-response-includes-batch-number]: "The database connection in the stock API path is resolved exclusively from the DATABASE_URL environment variable; no connection string or credential is hardcoded in any layer touched by this story"; T20 [ac AC1-response-includes-batch-number]: "fetching a stock record whose batch_number is set returns a response body that contains a batch_number field equal to that record's stored value"; T21 [ac AC2-response-includes-serial-number]: "The stock service module (app/services/) contains no ORM or session imports — it delegates all persistence to app/repositories/ and holds no SQLAlchemy Session references"; T22 [ac AC2-response-includes-serial-number]: "fetching a stock record whose serial_number is set returns a response body that contains a serial_number field equal to that record's stored value"; T23 [ac AC3-null-fields-preserved-as-null]: "The stock API boundary module (app/routes/) contains no ORM or session imports — the repository is the sole module that imports and operates the ORM session"; T24 [ac AC3-null-fields-preserved-as-null]: "fetching a stock record whose batch_number is NULL returns a response body where batch_number is explicitly null rather than omitted"; T25 [ac AC3-null-fields-preserved-as-null]: "fetching a stock record whose serial_number is NULL returns a response body where serial_number is explicitly null rather than omitted"; T26 [ac AC4-combined-code-not-returned]: "The stock endpoint returns a JSON response (Content-Type application/json) containing data fields — it does not return server-rendered HTML"; T27 [ac AC4-combined-code-not-returned]: "fetching any stock record after the tracking-code split returns a response body that does not contain an inventory_code field". Write ALL of them now and ONLY these; do NOT add or drop items, the orchestration stamps ONE whole-story batch RED cycle for exactly these ids, and any mismatch is a defect. TEST STATE OWNERSHIP (mandatory): each test OWNS the state it asserts on , the acceptance DB is a shared branch reused across the story's cycles and already holds rows other stories committed. For any COLLECTION or AGGREGATE assertion (an empty list/table, "returns all", a count), NEVER assume ambient state and NEVER assert absolute whole-table state (e.g. len(all) == 0): SCOPE to per-run-unique keys , assert only your own seeded rows, or query a per-run-unique slice that is genuinely empty , or explicitly clear the aggregate you claim empty. Also revert/roll back rows a test writes so it never leaks state into another test. UI track is ON: the UI must adhere to the project design guide at <PROJECT_ROOT>/.consort/design/design-guide.md (+ the design-guide.json tokens). Build to it. RUBRIC (pre-extracted; judge against THIS) :: layer=API | required NFRs, NFR-F6-2-no-negative-stock (N/A for this feature — the split-tracking refactor changes only the stock record's schema and display, not quantity math or the write path. The no-overcommit / non-negative guarantee (CHECK + service guard) established in F1 is preserved unchanged; this feature introduces no code that could drive quantity negative.); NFR-F6-4-real-branch-integration-tests (Every AC in this feature is verified by integration tests running against the paired Lakebase branch (databricks_postgres via DATABASE_URL), never a mock or in-memory substitute; migrations are applied to the branch first. CI refuses a PR whose integration tests do not run against a real branch.). The rubric above is pre-extracted from <PROJECT_ROOT>/.consort/features/F6-split-tracking-code/architecture.md, <PROJECT_ROOT>/.consort/nfrs.md, and <PROJECT_ROOT>/.consort/design/design-guide.md, open those full files ONLY if you need more detail than it carries (do not re-read them by default). LAYOUT (place/judge code at THESE paths, do not scan for them) :: boundary=app/routes | service=app/services | repository=app/repositories | models=app/models.