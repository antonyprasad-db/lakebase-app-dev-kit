Realize the physical database schema for story S1-file-stock-record into <PROJECT_ROOT>/.consort/features/F1-stock-visibility/db-design.json (+ a short db-design.md narrative). Story S1-file-stock-record's ACs are: AC1-file-creates-record, AC2-record-stores-tracking-code, AC3-same-pair-updates-in-place. Read architecture.json (service_backed, layers, persistence_invariants) , the architect owns that logical contract; you produce the PHYSICAL realization and do NOT re-author the invariants. Declare tables[] (columns with explicit type/nullable/default, primary_key, unique_constraints, foreign_keys, checks, indexes) and this story's schema_changes[] (the per-story migration plan the build lane authors the Alembic migration from; keep an expand/contract column split or drop reversible). Populate realizes_invariants[] as a flat array of the architecture.json persistence_invariant id STRINGS (bare ids, not objects) , an uncovered invariant hard-blocks the spec gate. This feature is service_backed. Mirror the architect's models package (app/models/), one table per domain object. Realize EVERY declared persistence_invariant and list its id in realizes_invariants[]: PI1-unique-sku-location [unique on stock_records] (Unique composite key on (sku, location) so no two rows share the same SKU-at-location pair; a repeat filing collides and updates in place (R3).); PI2-required-columns-not-null [not_null on stock_records] (sku, location, and quantity are NOT NULL so a filed record always carries its identity and level.); PI3-quantity-non-negative [check on stock_records] (CHECK quantity >= 0 so a stored stock level can never go below zero (R2).); PI4-migration-reversible [migration_reversible on stock_records] (The Alembic migration creating stock_records is additive and reversible, preserving pre-existing inventory rows across the upgrade (R1).). Write every artifact under the ABSOLUTE artifact root <PROJECT_ROOT>/.consort (this feature: <PROJECT_ROOT>/.consort/features/F1-stock-visibility/; this story: <PROJECT_ROOT>/.consort/features/F1-stock-visibility/stories/S1-file-stock-record/); use that absolute path and never resolve or guess the project root yourself.