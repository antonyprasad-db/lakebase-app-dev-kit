REVISE (reflection gate): blocking smell "reflect-spec-defect": reflection gate: acs/AC2-quantities-right-aligned.json, acs/AC3-same-sku-shown-per-location.json, and acs/AC4-empty-state-shown.json are invalid JSON: each file's top-level object closes correctly after `architectural_notes`, but is followed by orphaned trailing lines (`"distinct_from_prior": true, "rationale": ...` plus stray closing braces) left over from a botched edit. All three fail to parse (json.JSONDecodeError: Extra data), which will break any tooling that reads these AC files during the build lane.

Re-author this story's acceptance criteria to ADD the specific coverage named above. This coverage is REQUIRED: add the missing test(s)/criterion that assert the exact behavior described , do NOT omit it, weaken it, or defer it to an open question. If the stated behavior genuinely cannot be tested as written, name the concrete blocker; do not punt.