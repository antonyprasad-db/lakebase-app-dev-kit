REVISE (reflection gate): blocking smell "reflect-testlist-defect": reflection gate: test-list-per-story.json (and the master test-list.json) contains only T13-T15, all for AC1-table-lists-filed-stock. AC2-quantities-right-aligned, AC3-same-sku-shown-per-location, and AC4-empty-state-shown have zero covering tests, so the story cannot build to green on those ACs. AC2's own architectural_notes already name the missing test explicitly ('Covered by the client Vitest/Playwright harness ... rather than the branch integration suite'), so a client-kind test for right-alignment must be added; AC3 needs a behavior test seeding one SKU at two locations and asserting two distinct rows; AC4 needs a behavior test asserting the empty-state message on a zero-record branch.

Re-author this story's ordered test list to ADD the specific coverage named above. This coverage is REQUIRED: add the missing test(s)/criterion that assert the exact behavior described , do NOT omit it, weaken it, or defer it to an open question. If the stated behavior genuinely cannot be tested as written, name the concrete blocker; do not punt.