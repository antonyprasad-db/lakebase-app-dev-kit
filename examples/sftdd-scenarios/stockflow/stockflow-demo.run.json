{
  "id": "stockflow-demo",
  "description": "The 2-turn stockflow design demo, driven end to end: scaffold a real project (setup), replay the human PO seed, let a LIVE spec-author author the feature-spec, then tear the project down. Every value here is a DEFAULT , override any of them via the env var named in `${...}` so anyone can point the demo at their own workspace/owner and run it.",
  "setup": {
    "kind": "scaffold-project",
    "config": {
      "projectName": "${STOCKFLOW_DEMO_PROJECT:-stockflow-demo}",
      "databricksHost": "${DATABRICKS_HOST:-https://fevm-serverless-stable-ecparr.cloud.databricks.com}",
      "githubOwner": "${STOCKFLOW_DEMO_GH_OWNER:-kevin-hartman}",
      "createGithubRepo": "${STOCKFLOW_DEMO_CREATE_REPO:-true}",
      "tiers": "${STOCKFLOW_DEMO_TIERS:-1}",
      "uiTrack": "${STOCKFLOW_DEMO_UI:-true}",
      "language": "${STOCKFLOW_DEMO_LANGUAGE:-nodejs}"
    }
  },
  "start": { "kind": "invoke-role", "role": "product-owner", "mode": "author-requests" },
  "teardown": {
    "kind": "remove-project",
    "config": {}
  }
}
