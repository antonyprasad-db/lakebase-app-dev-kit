{
  "id": "AC2-quantity-right-aligned",
  "given": "the home table is showing rows of stock by location",
  "when": "the warehouse team member reads the quantity column",
  "then": "each quantity value is right-aligned within its column so the digits line up for at-a-glance scanning",
  "status": "draft",
  "layer": "E2E",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC1 asserts that the rows and their SKU/location/quantity values appear at all; AC2 asserts the distinct visual outcome that quantities are right-aligned. Alignment can break (left-aligned digits) while rows still render correctly, and rows can be missing while any single quantity that does render is right-aligned, so each fails independently."
  },
  "architectural_notes": "E2E: a pure presentation concern owned entirely by the React SPA (client/) quantity-column component; no backend layer participates beyond the JSON the boundary (app/routes/) already returns. No persistence and no service/repository logic is touched, alignment is styling, not data. Cross-cutting: none server-side; the layout rule stays in the client component and must not leak into the JSON contract. Verified through the client's component/E2E harness asserting the quantity cells render right-aligned."
}
