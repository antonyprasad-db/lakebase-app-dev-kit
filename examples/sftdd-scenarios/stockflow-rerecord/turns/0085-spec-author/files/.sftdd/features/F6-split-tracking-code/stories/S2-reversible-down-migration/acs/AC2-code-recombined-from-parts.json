{
  "id": "AC2-code-recombined-from-parts",
  "given": "a stock record with a canonical location and non-null batch_number and serial_number",
  "when": "the down path reconstructs its inventory_code",
  "then": "the inventory_code value is the canonical location, batch, and serial rejoined in that order by the hyphen delimiter",
  "status": "draft",
  "layer": "Infra",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC1 only asserts the combined column reappears; this asserts the reconstructed value's content is composed correctly from location plus batch plus serial, which can be failed independently by recreating an empty or wrongly-ordered code while the column still exists."
  }
}
