{
  "id": "AC2-nonconforming-code-left-null",
  "status": "draft",
  "layer": "Infra",
  "given": "a sprint-1 stock row whose inventory_code has no batch or serial segment (for example \"X-1\" or a bare \"c\")",
  "when": "the up migration runs and backfills the new columns",
  "then": "that row's batch_number and serial_number are left NULL rather than guessed or defaulted, and the row still survives the migration and is retrievable",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC1 asserts a conforming code parses into populated batch/serial; AC2 asserts the opposite branch, a code that does not parse, is left NULL and not dropped. Backfill logic that parses conforming codes could still incorrectly guess or discard nonconforming ones, so this fails independently."
  },
  "architectural_notes": "Infra layer: the same up revision on the shared branch, read back via app/repositories. Realizes PI3 (batch_number/serial_number added NULLABLE, no NOT NULL) and PI4 (row survival): the parse in app/services must emit NULL for unparseable codes rather than defaulting or forcing the row out, so the columns carry no NOT NULL constraint and the row persists. Owner modules: migrations/versions/*, app/services (parse null-branch), app/models/stock.py. Verified by a kind:behavior real-branch test seeded with nonconforming codes on the shared branch in its UP state."
}
