{
  "id": "AC2-quantity-right-aligned",
  "status": "draft",
  "layer": "E2E",
  "given": "the home stock-by-location table is rendering stock rows",
  "when": "the operator reads the quantity column",
  "then": "each row's quantity value is right-aligned within its column so the digits line up for at-a-glance scanning",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "AC1 asserts the rows and their data are present; AC2 asserts the distinct presentation outcome that quantities are right-aligned, which can break (left/center aligned) while AC1's rows still render correctly."
  }
}
