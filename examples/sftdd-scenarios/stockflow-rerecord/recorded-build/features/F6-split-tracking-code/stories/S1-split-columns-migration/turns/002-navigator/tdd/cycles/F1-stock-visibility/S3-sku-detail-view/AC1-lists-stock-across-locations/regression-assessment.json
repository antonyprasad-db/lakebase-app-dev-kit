{
  "diagnosis": "Two distinct regressions. (1) T25 (tests/test_S3_fitness.py::test_T25_sku_detail_read_path_layering): the fitness test asserts the service defines a function containing 'get_sku_detail' or 'sku_detail', but the Driver named it list_stock_by_sku -- neither substring matches. (2) T26/T28/T30/T32 (client/tests/pages/SkuDetail.test.tsx): the test imports SkuDetailPage from client/src/pages/SkuDetailPage.tsx (file does not exist) and mocks/imports fetchSkuDetail from client/src/api/stock (not exported) -- the Driver implemented the backend only and left the client layer unbuilt, matching the identical pattern from S1 and S2 prior regression assessments.",
  "fixDirective": "1) In app/services/stock_service.py rename list_stock_by_sku to get_sku_detail (or add an alias) and update the route import/call in app/routes/stock.py and the repository call accordingly. 2) Export fetchSkuDetail(sku: string): Promise<SkuDetailEntry[]> from client/src/api/stock.ts (GET /api/stock/sku/:sku). 3) Create client/src/pages/SkuDetailPage.tsx rendering data-testid seams: sku-detail-table, sku-detail-row-{location}, sku-detail-qty-{location}, sku-detail-code-{location}, sku-detail-par-{location} (showing 'not tracked' when par_level is null), and empty-state with text 'No stock for this SKU' when the collection is empty."
}
